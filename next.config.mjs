/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    basePath: "/Stranger-Things-Website",
    reactStrictMode: true,
    images: {
        unoptimized: true,
    },
};

export default nextConfig;
